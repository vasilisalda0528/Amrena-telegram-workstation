import './styles.css'

const Button = ({ title }) => {
    return (
        <div className='temp-div'>
            <div style={{color:'#6F3716'}}>{title}</div>
            {/* <img src={btn} /> */}
            </div>
    );
  };

export default Button